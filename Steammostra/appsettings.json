{
  "AllowedHosts": "*",
  // Objeto que contém as strings de conexão para o banco de dados.
  "ConnectionStrings": {
    "DefaultConnection": "server=localhost;port=3306;database=mostrasteam;user=root;password=cimatec"
  },
  // Configurações de logging (registro de logs) para o aplicativo.
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}